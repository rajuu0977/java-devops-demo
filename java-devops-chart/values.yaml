replicaCount: 2

image:
  repository: java-devops-demo
  pullPolicy: Never
  tag: "1.0"

service:
  type: NodePort
  port: 8080

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: ""
      paths:
        - path: /
          pathType: Prefix

resources: {}
